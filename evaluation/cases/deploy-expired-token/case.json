{
  "caseId": "deploy-expired-token",
  "title": "Phase 2 control 2+3 — token rác/hết hạn → -2001 → needs_login; token không lọt evidence",
  "expectation": {
    "exitCode": 2,
    "resultStatus": "needs_input",
    "findingCategory": null,
    "findingSlugHint": "login_required",
    "description": ".env có ZMP_TOKEN=<JWT-shaped rác sinh runtime> → deploy.mjs spawn thật zmp deploy (an toàn: server từ chối -2001) → exit 2 needs_login, deploy.log tồn tại, giá trị rác KHÔNG xuất hiện ở bất kỳ artifact nào dưới runs/ (redaction trên đường deploy thật). Cần zmp binary + mạng tới zmp-api.developers.zalo.me; thiếu → blocked."
  }
}
