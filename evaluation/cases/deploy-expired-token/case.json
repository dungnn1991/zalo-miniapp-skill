{
  "caseId": "deploy-expired-token",
  "title": "Phase 2 control 2+3 — token rác/hết hạn → -2001 → needs_login; token không lọt evidence",
  "expectation": {
    "exitCode": 2,
    "resultStatus": "needs_input",
    "findingCategory": null,
    "findingSlugHint": "login_required",
    "description": ".env có ZMP_TOKEN=<JWT-shaped rác sinh runtime> → deploy.mjs spawn executable zmp cô lập do case tạo, fake CLI echo token rồi trả auth failure đã pin → exit 2 needs_login; deploy.log tồn tại và chỉ chứa [REDACTED]. Không cần zmp-cli hay network live."
  }
}
